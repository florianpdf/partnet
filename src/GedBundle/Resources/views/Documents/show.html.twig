{% extends '::base.html.twig' %}

{% block main_content -%}
    <h1>Documents</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ entity.titre }}</td>
            </tr>
            <tr>
                <th>Resume</th>
                <td>{{ entity.resume }}</td>
            </tr>
            <tr>
                <th>Extension</th>
                <td>{{ entity.extension }}</td>
            </tr>
            <tr>
                <th>Auteur</th>
                <td>{{ entity.auteur }}</td>
            </tr>
            <tr>
                <th>Url</th>
                <td>{{ entity.file }}</td>
            </tr>
            <tr>
                <th>Dateupload</th>
                <td>{{ entity.dateUpload|date('Y-m-d H:i:s') }}</td>
            </tr>
            <tr>
                <th>Datemodif</th>
                <td>{{ entity.dateModif|date('Y-m-d H:i:s') }}</td>
            </tr>
            <tr>
                <th>Findevie</th>
                <td>{{ entity.finDeVie|date('Y-m-d H:i:s') }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('documents') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('documents_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
